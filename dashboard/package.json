{
  "name": "bytebrains-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync-bundle": "node scripts/sync-bundle.mjs",
    "predev": "npm run sync-bundle",
    "dev": "next dev",
    "prebuild": "npm run sync-bundle",
    "build": "next build",
    "test": "vitest run",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "echarts": "^5.6.0",
    "next": "^15.5.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
